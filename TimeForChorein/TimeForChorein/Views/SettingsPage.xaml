<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TimeForChorein.Views.SettingsPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Populate mock data"
                   HorizontalOptions="CenterAndExpand"
                   FontSize="Large" />
            <Button x:Name="PopulateDataButton"
                    Command="{Binding PopulateDataButton_Clicked}"
                    HorizontalOptions="FillAndExpand"
                    Text="{Binding PopulateDataButtonText}" />
            <Label Text="{Binding UpdateMessage}"                   
                   HorizontalOptions="CenterAndExpand"
                   FontSize="Large"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>